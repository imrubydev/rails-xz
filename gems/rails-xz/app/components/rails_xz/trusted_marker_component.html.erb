<span class="xz-trusted-marker" data-claim="<%= claim %>">
  <strong class="xz-trusted-marker__label">@trusted</strong>
  <span class="xz-trusted-marker__claim"><%= claim %></span>
  <% if note %>
    <span class="xz-trusted-marker__note"><%= note %></span>
  <% end %>
</span>